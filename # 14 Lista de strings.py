# 14 Lista de strings
lista_strings = ['casa', 'carro', 'abacate', 'pipoca']

# Ordenação da lista por tamanho em ordem decrescente
lista_ordenada = sorted(lista_strings, key=len, reverse=True)

# Exibindo o resultado
print("Lista ordenada por ordem decrescente de tamanho:", lista_ordenada)